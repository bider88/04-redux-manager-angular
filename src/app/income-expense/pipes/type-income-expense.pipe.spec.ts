import { TypeIncomeExpensePipe } from './type-income-expense.pipe';

describe('TypeIncomeExpensePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeIncomeExpensePipe();
    expect(pipe).toBeTruthy();
  });
});
